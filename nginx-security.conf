# Nginx Security Hardening — dimuat dari nginx.conf (http block)

# Rate limiting (global — per IP, 10 req/detik)
limit_req_zone $binary_remote_addr zone=global:10m rate=10r/s;

# Batasi koneksi bersamaan per IP
limit_conn_zone $binary_remote_addr zone=connperip:10m;
